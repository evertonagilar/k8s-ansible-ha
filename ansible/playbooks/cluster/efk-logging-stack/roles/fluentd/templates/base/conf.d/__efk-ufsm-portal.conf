<source>
  @type tail
  @id ${ENV}-portal
  path /var/log/containers/${ENV}-portal-*-sieweb_portal-*.log
  pos_file /var/log/fluentd.${ENV}-portal.log.pos
  tag ${ENV}-portal
  read_from_head false
  format multiline
  format /^.+stdout F (?<log>.+)$/
</source>
<filter ${ENV}-portal>
  @type grep
  <exclude>
    key log
    pattern /Broken pipe|client prematurely closed/
  </exclude>
</filter>
<filter ${ENV}-portal>
  @type parser
  key_name log
  format /^(?<utc_timestamp>\d{2}\/\w+\/\d{4}:\d{2}:\d{2}:\d{2} \+\d{4}) - Host: (?<host>[^\s]+) Request: (?<method>[A-Z]+) (?<url>[^\s]+) (?<protocol>[^\s]+) Status: (?<status>\d+) BytesSend: (?<bytesSend>\d+) - RemoteIP: (?<remote_ip>[^\s]+) ForwardFor: (?<forward_for>[^s]+) RemoteUser: (?<remote_user>[^\s]+) User-agent: "(?<user_agent>[^"]*)" Backend=(?<backend>[^\$]+)/
</filter>
# <filter ${ENV}-portal>
#  @type kubernetes_metadata
#  @id filter_kube_metadata
# </filter>
<match ${ENV}-portal>
   @type elasticsearch
   @id out_es_${ENV}_portal
   @log_level info
   #include_tag_key true
   host "#{ENV['FLUENT_ELASTICSEARCH_HOST']}"
   port "#{ENV['FLUENT_ELASTICSEARCH_PORT']}"
   path "#{ENV['FLUENT_ELASTICSEARCH_PATH']}"
   user "#{ENV['FLUENT_ELASTICSEARCH_USER'] || ''}"
   password "#{ENV['FLUENT_ELASTICSEARCH_PASSWORD'] || ''}"
   scheme "#{ENV['FLUENT_ELASTICSEARCH_SCHEME'] || 'http'}"
   ssl_verify "#{ENV['FLUENT_ELASTICSEARCH_SSL_VERIFY'] || 'true'}"
   ssl_version "#{ENV['FLUENT_ELASTICSEARCH_SSL_VERSION'] || 'TLSv1_2'}"
   reload_connections "#{ENV['FLUENT_ELASTICSEARCH_RELOAD_CONNECTIONS'] || 'false'}"
   reconnect_on_error "#{ENV['FLUENT_ELASTICSEARCH_RECONNECT_ON_ERROR'] || 'true'}"
   reload_on_failure "#{ENV['FLUENT_ELASTICSEARCH_RELOAD_ON_FAILURE'] || 'true'}"
   log_es_400_reason "#{ENV['FLUENT_ELASTICSEARCH_LOG_ES_400_REASON'] || 'false'}"
   logstash_prefix "#{ENV['FLUENT_ELASTICSEARCH_LOGSTASH_PREFIX'] || '${ENV}-portal'}"
   logstash_format "#{ENV['FLUENT_ELASTICSEARCH_LOGSTASH_FORMAT'] || 'true'}"
   index_name "#{ENV['FLUENT_ELASTICSEARCH_LOGSTASH_INDEX_NAME'] || '${ENV}-portal'}"
   type_name "#{ENV['FLUENT_ELASTICSEARCH_LOGSTASH_TYPE_NAME'] || 'fluentd'}"
   <buffer>
     flush_thread_count "#{ENV['FLUENT_ELASTICSEARCH_BUFFER_FLUSH_THREAD_COUNT'] || '8'}"
     flush_interval "#{ENV['FLUENT_ELASTICSEARCH_BUFFER_FLUSH_INTERVAL'] || '5s'}"
     chunk_limit_size "#{ENV['FLUENT_ELASTICSEARCH_BUFFER_CHUNK_LIMIT_SIZE'] || '2M'}"
     queue_limit_length "#{ENV['FLUENT_ELASTICSEARCH_BUFFER_QUEUE_LIMIT_LENGTH'] || '32'}"
     retry_max_interval "#{ENV['FLUENT_ELASTICSEARCH_BUFFER_RETRY_MAX_INTERVAL'] || '30'}"
     retry_forever true
   </buffer>
</match>
